# دليل استخدام مكون StarField ✨

## الخصائص المتاحة

### 1. **starCount** - عدد النجوم
- **الافتراضي**: 180
- **النطاق المقترح**: 50 - 500
- **الوصف**: يحدد عدد النجوم المعروضة

```tsx
<StarField starCount={100} /> // قليل
<StarField starCount={250} /> // متوسط  
<StarField starCount={400} /> // كثيف
```

### 2. **speed** - سرعة الحركة
- **الافتراضي**: 0.2
- **النطاق المقترح**: 0.05 - 1.0
- **الوصف**: يحدد سرعة حركة النجوم

```tsx
<StarField speed={0.05} /> // بطيء جداً
<StarField speed={0.2} />  // عادي
<StarField speed={0.5} />  // سريع
<StarField speed={1.0} />  // سريع جداً
```

### 3. **twinkleSpeed** - سرعة التلألؤ
- **الافتراضي**: 0.012
- **النطاق المقترح**: 0.005 - 0.05
- **الوصف**: يحدد سرعة تلألؤ النجوم

```tsx
<StarField twinkleSpeed={0.005} /> // تلألؤ بطيء
<StarField twinkleSpeed={0.012} /> // تلألؤ عادي
<StarField twinkleSpeed={0.03} />  // تلألؤ سريع
```

### 4. **minSize & maxSize** - حجم النجوم
- **الافتراضي**: minSize=0.3, maxSize=2.8
- **النطاق المقترح**: 0.1 - 5.0
- **الوصف**: يحدد النطاق لأحجام النجوم

```tsx
{/* نجوم صغيرة */}
<StarField minSize={0.2} maxSize={1.0} />

{/* نجوم متوسطة */}
<StarField minSize={0.5} maxSize={2.5} />

{/* نجوم كبيرة */}
<StarField minSize={1.0} maxSize={4.0} />
```

## أمثلة للاستخدام

### 🌌 سماء هادئة
```tsx
<StarField 
  starCount={150}
  speed={0.08}
  twinkleSpeed={0.008}
  minSize={0.3}
  maxSize={1.5}
  className="opacity-70"
/>
```

### ⭐ سماء نشطة
```tsx
<StarField 
  starCount={300}
  speed={0.3}
  twinkleSpeed={0.02}
  minSize={0.5}
  maxSize={3.0}
  className="opacity-90"
/>
```

### 🌟 سماء مليئة بالنجوم
```tsx
<StarField 
  starCount={500}
  speed={0.15}
  twinkleSpeed={0.015}
  minSize={0.2}
  maxSize={2.0}
  className="opacity-80"
/>
```

### 💫 نجوم كبيرة وبطيئة
```tsx
<StarField 
  starCount={80}
  speed={0.05}
  twinkleSpeed={0.005}
  minSize={1.0}
  maxSize={4.0}
  className="opacity-60"
/>
```

## نصائح للاستخدام

### الأداء 🚀
- لأفضل أداء، استخدم `starCount` أقل من 400
- النجوم الكبيرة تستهلك موارد أكثر

### التصميم 🎨
- استخدم `opacity-70` إلى `opacity-90` للحصول على أفضل تأثير
- النجوم الصغيرة والسريعة تعطي إحساس بالحيوية
- النجوم الكبيرة والبطيئة تعطي إحساس بالهدوء

### التوافق 📱
- يعمل على جميع الشاشات والأجهزة
- يتكيف تلقائياً مع تغيير حجم النافذة
